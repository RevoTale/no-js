package routes

import "example.com/no-js-e2e/routepagecssapp/web/view"

css pageShell() {
	padding: 16px;
	border: 1px solid #123;
}

templ Page(model view.RootPageView) {
	<main data-route-page="home" class={ pageShell() }>
		<h1>{ model.Heading }</h1>
	</main>
}
