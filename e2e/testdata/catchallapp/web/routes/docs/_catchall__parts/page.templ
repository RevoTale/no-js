package parts

import "example.com/no-js-e2e/catchallapp/web/view"

css docsPage() {
	background: #eef5ff;
	padding: 12px;
}

templ Page(view runtime.CatchAllPageView) {
	<main id="docs-catchall" class={ docsPage() }>
		<p data-joined>{ view.Joined }</p>
		<p data-depth>{ view.Depth }</p>
	</main>
}
